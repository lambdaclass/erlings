-module(priority).

-export([start/0,
         get_messages/1,
         priority_loop/1]).

start() ->
  your_solution_here.

get_messages(Pid) ->
  your_solution_here.

priority_loop(State) ->
  your_solution_here.
