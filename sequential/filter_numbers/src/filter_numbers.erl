-module(filter_numbers).

-export([filter_in/3,
         filter_out/3,
         filter_in_values/3]).

filter_in(From, To, N) ->
  put_your_solution_here.

filter_out(From, To, N) ->
  put_your_solution_here.

filter_in_values(From, To, {Min, Max}) ->
  put_your_solution_here.
